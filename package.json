{
  "name": "runcheck",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "test": "vitest --ui",
    "test:run": "vitest run",
    "build": "pnpm test:run && tsc -p tsconfig.prod.json",
    "npm-publish": "pnpm build && npm publish"
  },
  "files": [
    "dist"
  ],
  "packageManager": "^pnpm@7.6.0",
  "repository": "github:lucasols/runcheck",
  "author": "Lucas Santos",
  "main": "./dist/runcheck.js",
  "types": "./dist/runcheck.d.ts",
  "sideEffects": false,
  "engines": {
    "node": ">=14.6.0"
  },
  "devDependencies": {
    "@types/node": "^18.7.2",
    "@vitest/ui": "^0.21.1",
    "typescript": "^4.8.1-rc",
    "vite": "^3.0.7",
    "vitest": "^0.21.1",
    "zod": "^3.18.0"
  }
}
